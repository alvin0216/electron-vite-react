import { Button } from 'antd';

interface ToggleWriteableProps {}

const ToggleWriteable: React.FC<ToggleWriteableProps> = (props) => {
  return <Button>Readonly</Button>;
};

export default ToggleWriteable;
